name: X Auto Post (Every 5 hours)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */5 * * *"

permissions:
  contents: write

jobs:
  autopost:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install requests requests-oauthlib Pillow

      - name: Run autopost
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
          CMC_KEY: ${{ secrets.CMC_KEY }}
        run: |
          echo "Starting X Auto Post: $(date -u)"
          python scripts/x_autopost.py

      - name: Commit logs (tweet + state)
        run: |
          set -e
          git config user.name "x-bot"
          git config user.email "x-bot@users.noreply.github.com"

          git stash push -m "autopost-stash" || true
          git pull --rebase origin main || true
          git stash pop || true

          git add out/tweet.txt out/last_post.json out/cmc_debug.json
          git commit -m "x: auto post $(date -u)" || echo "Nothing to commit"
          git push origin HEAD:main || true
