name: Sync X Reports (#WPO_REPORT)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"  # hourly (UTC)

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Sync reports.json from X
        env:
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          X_USERNAME: "Protocol_WPO"
          X_FILTER: "#WPO_REPORT"
          X_MAX_RESULTS: "50"
          OUTPUT_PATH: "reports.json"
        run: python scripts/sync_x_reports.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add reports.json
          git diff --staged --quiet || git commit -m "chore: sync reports from X"
          git push
